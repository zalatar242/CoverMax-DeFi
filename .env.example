# Network & Fork Configuration
NETWORK=mainnet            # Options: mainnet, sepolia
FORK_ENABLED=true         # Set to true to use Base fork

# RPC URLs - Get Base RPC URLs from https://docs.base.org/guides/quickstart
BASE_MAINNET_RPC_URL=     # Required for Base mainnet or mainnet fork
BASE_SEPOLIA_RPC_URL=     # Required for Base Sepolia or Sepolia fork

# Live Network Deployment (only needed for deploying to actual Base networks)
PRIVATE_KEY=              # Your deployer wallet private key
BASESCAN_API_KEY=         # Get from https://basescan.org/apis

# Testing Configuration (only needed for running tests)
MAINNET_USDC_WHALE=       # USDC whale address for mainnet fork tests
